#!/usr/bin/env bash

cd "$(dirname "$0")/.."

if [ ! -x .venv/bin/python ]; then
	echo "Missing .venv Python. Run scripts/setup first." >&2
	exit 1
fi

exec .venv/bin/python src/carrier_api/live_smoke_test.py "$@"
